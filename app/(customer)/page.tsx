import { CatalogBrowser } from "@/app/components/CatalogBrowser";

/** Home (§6.1) — all doors by default, with search + filters. */
export default function HomePage() {
  return <CatalogBrowser />;
}
